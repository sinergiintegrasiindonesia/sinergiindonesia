node_modules/
.vercel
.DS_Store

# Brand master artwork — kept locally, not deployed
Logo/

# Generated build output — not deployed
build/

# Real SMTP credentials — the sample template is tracked, the real one never is
smtp-config.php
